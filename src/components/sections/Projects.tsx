"use client";

export default function Projects() {
    return (
        <section style={{maxWidth: "min(92vw, 840px)", margin: "5rem auto", textAlign: "center"}}>
            <h2 style={{fontWeight: 700, marginBottom: 12}}>Projects</h2>
            <p style={{opacity: 0.9}}>
                I’m curating this section. Check back soon for selected work in identity,
                DX tooling, and clean UI systems.
            </p>
        </section>
    );
}
